// PLACEHOLDER — replaced by worker-mcp
/**
 * Contract expected by `src/index.ts`:
 *
 *   `startServer()` builds the `McpServer`, registers exactly seven tools and
 *   connects `StdioServerTransport`. It resolves when the transport closes and
 *   rejects on a startup failure. `installStdoutGuard()` has already run.
 */
export async function startServer(): Promise<void> {
  throw new Error('NOT_IMPLEMENTED: server (owned by worker-mcp)');
}
